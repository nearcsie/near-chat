name: CI Browser
# Browser testing lanes:
#   browser-tests: Frontend smoke tests with mocked API.
#   fullstack-browser-tests: Full-stack E2E with real Next.js, Bun, PostgreSQL, and Socket.IO.

on:
  workflow_call:

permissions:
  contents: read

jobs:
  browser-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1
      - uses: ./.github/actions/setup-workspace
        with:
          package: near-chat-frontend

      - name: Get Playwright version
        id: playwright-version
        run: |
          echo "version=$(pnpm --filter near-chat-frontend exec playwright --version | awk '{print $2}')" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browser binaries
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}

      # Install Chromium browser binary and system dependencies.
      - name: Install Chromium and system dependencies
        run: |
          if [ "${{ steps.playwright-cache.outputs.cache-hit }}" = "true" ]; then
            pnpm --filter near-chat-frontend exec playwright install-deps chromium
          else
            pnpm --filter near-chat-frontend exec playwright install --with-deps chromium
          fi

      - name: Run browser smoke tests
        run: pnpm --filter near-chat-frontend test:browser

      - name: Output browser tests summary
        run: |
          echo "### 🌐 Browser Smoke Tests Passed" >> "$GITHUB_STEP_SUMMARY"
          echo "- Environment: Chromium (headless) against mock API" >> "$GITHUB_STEP_SUMMARY"
          echo "- Playwright cache hit: \`${{ steps.playwright-cache.outputs.cache-hit == 'true' }}\`" >> "$GITHUB_STEP_SUMMARY"

      # Upload diagnostics only when tests fail.
      - name: Upload Playwright diagnostics
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: |
            frontend/playwright-report/
            frontend/test-results/
          retention-days: 7
          if-no-files-found: ignore

  # Full-stack browser E2E (issue #544): Chromium → Next.js → Hono/Bun →
  # PostgreSQL → Socket.IO, with nothing mocked.
  fullstack-browser-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      # Same image, port and health options as ci-database.yml. Reused rather
      # than reinvented, and deliberately not Docker Compose: this lane needs an
      # ephemeral database, which is exactly what a service container is.
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ntnu_test
        ports:
          - 5433:5432
        # Named user and database, matching ci-database.yml: a bare
        # `pg_isready` probes as the container's own root user and fills the
        # service container log with `FATAL: role "root" does not exist` every
        # 2s, which buries real errors when diagnosing a failed run.
        options: >-
          --health-cmd "pg_isready -U postgres -d ntnu_test"
          --health-interval 2s
          --health-timeout 5s
          --health-retries 10
    env:
      # Use loopback IP across services to prevent SameSite cookie boundary issues.
      DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5433/ntnu_test
      E2E_API_ORIGIN: http://127.0.0.1:4000
      E2E_FRONTEND_ORIGIN: http://127.0.0.1:3000
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - uses: ./.github/actions/setup-workspace
        with:
          bun: 'true'
      - run: pnpm install --frozen-lockfile

      - name: Get Playwright version
        id: playwright-version
        run: |
          echo "version=$(pnpm --filter near-chat-frontend exec playwright --version | awk '{print $2}')" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browser binaries
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}

      - name: Install Chromium and system dependencies
        run: |
          if [ "${{ steps.playwright-cache.outputs.cache-hit }}" = "true" ]; then
            pnpm --filter near-chat-frontend exec playwright install-deps chromium
          else
            pnpm --filter near-chat-frontend exec playwright install --with-deps chromium
          fi

      - name: Migrate database
        run: pnpm --filter near-chat-backend migrate:up

      # Start backend in development mode to support unencrypted local test cookies.
      - name: Start backend
        env:
          NODE_ENV: development
          PORT: '4000'
          JWT_SECRET: ci-fullstack-browser-e2e-secret
          CORS_ORIGINS: ${{ env.E2E_FRONTEND_ORIGIN }}
          REDIS_URL: ''
          RATE_LIMIT_DISABLED: 'true'
        run: |
          pnpm --filter near-chat-backend start > backend.log 2>&1 &
          echo "$!" > backend.pid

      # Wait for backend health endpoint to respond before continuing.
      - name: Wait for backend
        run: ./scripts/wait-for-service.sh Backend backend.pid "$E2E_API_ORIGIN/api/v1/health" backend.log

      - name: Build frontend
        env:
          NEXT_PUBLIC_API_URL: ${{ env.E2E_API_ORIGIN }}
        run: pnpm --filter near-chat-frontend exec next build

      - name: Start frontend
        env:
          NEXT_PUBLIC_API_URL: ${{ env.E2E_API_ORIGIN }}
        run: |
          pnpm --filter near-chat-frontend exec next start --hostname 127.0.0.1 --port 3000 > frontend.log 2>&1 &
          echo "$!" > frontend.pid

      - name: Wait for frontend
        run: ./scripts/wait-for-service.sh Frontend frontend.pid "$E2E_FRONTEND_ORIGIN/login" frontend.log

      - name: Run full-stack browser tests
        run: pnpm --filter near-chat-frontend test:browser:fullstack

      - name: Output full-stack browser tests summary
        run: |
          echo "### 🌐 Full-Stack Browser E2E Tests Passed" >> "$GITHUB_STEP_SUMMARY"
          echo "- Stack: Chromium ➔ Next.js ➔ Hono/Bun ➔ PostgreSQL 18" >> "$GITHUB_STEP_SUMMARY"
          echo "- Playwright cache hit: \`${{ steps.playwright-cache.outputs.cache-hit == 'true' }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Real database, authentication, session cookies, and realtime events verified." >> "$GITHUB_STEP_SUMMARY"

      - name: Stop application processes
        if: always()
        run: |
          for pidfile in backend.pid frontend.pid; do
            [ -f "$pidfile" ] || continue
            kill "$(cat "$pidfile")" 2>/dev/null || true
          done

      # Redact auth tokens and cookies from traces before uploading diagnostics.
      - name: Redact credentials from Playwright diagnostics
        id: redact-playwright-diagnostics
        if: failure()
        run: |
          python3 scripts/redact_playwright_traces.py frontend/test-results-fullstack
          python3 scripts/redact_playwright_traces.py frontend/playwright-report-fullstack

      - name: Upload full-stack diagnostics
        if: failure() && steps.redact-playwright-diagnostics.outcome == 'success'
        uses: actions/upload-artifact@v7
        with:
          name: playwright-fullstack-diagnostics
          path: |
            frontend/playwright-report-fullstack/
            frontend/test-results-fullstack/
            backend.log
            frontend.log
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload application logs when diagnostic redaction fails
        if: failure() && steps.redact-playwright-diagnostics.outcome != 'success'
        uses: actions/upload-artifact@v7
        with:
          name: fullstack-application-logs
          path: |
            backend.log
            frontend.log
          retention-days: 7
          if-no-files-found: ignore
