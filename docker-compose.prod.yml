# Ingress topology: cloudflared is the only way in from the internet, and it
# reaches `frontend` and `backend` over this compose network rather than through
# any published host port. Every published port below is therefore bound to
# 127.0.0.1 — enough for the documented local production walkthrough (browse
# http://localhost:3005), and unreachable from the network, so nothing can skip
# the tunnel to talk to the backend or the database directly.
#
# That binding is what makes trusting `X-Forwarded-For` safe here: see
# TRUST_PROXY_HOPS on the backend service.
services:
  db:
    image: postgres:18-alpine
    ports:
      - "127.0.0.1:5435:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:8-alpine
    # Deliberately publishes no host port. Unlike `db`, nothing in the documented
    # local production walkthrough inspects Redis from the host, and this Redis
    # has no password — reachability from the compose network only *is* the
    # access control. Publishing a port here is never the right fix; exec into
    # the container instead.
    #
    # Persistence off and eviction left at `noeviction` for the same reasons as
    # the dev stack — see docker-compose.yml for the full rationale.
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    tmpfs:
      - /data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep -q PONG"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: .
      dockerfile: ./backend/Dockerfile.prod
    ports:
      - "127.0.0.1:4005:4000"
    environment:
      - NODE_ENV=${NODE_ENV}
      - PORT=${PORT}
      - DATABASE_URL=${DATABASE_URL}
      # Interpolated rather than pinned literally, unlike TRUST_PROXY_HOPS below.
      # That one is literal because production's correct value *differs* from
      # what a shared .env carries, so an inherited value would silently be
      # wrong. This one resolves to the same string in both stacks (same service
      # name, same container port), and pointing a deployment at a managed Redis
      # — usually `rediss://` — is an explicit requirement of issue #471.
      #
      # `-` rather than `:-`, for the same reason as docker-compose.yml: an
      # empty REDIS_URL reaches the container empty, and the backend reads that
      # as "no Redis" and runs realtime single-node — a supported deployment,
      # not a misconfiguration to be defaulted away.
      - REDIS_URL=${REDIS_URL-redis://redis:6379}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRES_IN=${JWT_EXPIRES_IN}
      - RATE_LIMIT_DISABLED=${RATE_LIMIT_DISABLED}
      # Pass-through only; the logger defaults to info when this is unset.
      - LOG_LEVEL
      # One trusted hop: cloudflared, which appends the caller's address to
      # X-Forwarded-For. Without it every request through the tunnel is attributed
      # to the tunnel container, so a single caller exhausting the auth limiter
      # (10 attempts / 15 min) locks out logins for everyone.
      #
      # Written literally rather than as ${TRUST_PROXY_HOPS:-1}: Compose
      # interpolates from the project .env, so a value carried over from
      # .env.example would quietly replace this default and the deployment would
      # be back to one shared bucket. Change it here, next to the topology that
      # determines it — add a hop for each additional proxy you put in front, and
      # verify with the procedure in docs/DEVELOPMENT.md.
      - TRUST_PROXY_HOPS=1
      - CORS_ORIGINS=${CORS_ORIGINS}
      - ATTACHMENT_TYPE_RESTRICTION_ENABLED=${ATTACHMENT_TYPE_RESTRICTION_ENABLED:-false}
      - ATTACHMENT_ALLOWED_MIME_TYPES=${ATTACHMENT_ALLOWED_MIME_TYPES:-image/jpeg,image/png,image/gif,application/pdf,application/zip,text/plain}
      - ATTACHMENT_ALLOWED_EXTENSIONS=${ATTACHMENT_ALLOWED_EXTENSIONS:-.jpg,.jpeg,.png,.gif,.pdf,.zip,.txt}
      - ATTACHMENT_MAX_BYTES=${ATTACHMENT_MAX_BYTES:-10485760}
      - MAX_SESSIONS_PER_USER=${MAX_SESSIONS_PER_USER:-5}
      - PRESENCE_GRACE_MS=${PRESENCE_GRACE_MS:-3000}
      # How long a presence lease survives in Redis unrefreshed. It bounds how
      # long a crashed instance keeps its users showing as online, because
      # nothing runs on a dead process to hand the leases back.
      - PRESENCE_TTL_MS=${PRESENCE_TTL_MS:-30000}
      - TYPING_TTL_MS=${TYPING_TTL_MS:-3000}
      # Pass-through only, like LOG_LEVEL: unset means the backend generates one
      # per process. Worth setting where the orchestrator already has a stable
      # per-replica name — a restarted process that reclaims its old name
      # replaces its own presence lease instead of adding a second one that has
      # to wait out the TTL.
      - INSTANCE_ID
      # Pass-through only, for the same reason: unset means the unscoped
      # `near-chat-ws` channel, which is right for a stack that owns its `redis`
      # service. Set it, differently per deployment, only when two stacks share
      # one Redis — pub/sub is not separated by the logical database, so a
      # distinct REDIS_URL database does not keep them apart.
      - REALTIME_CLUSTER_ID
      - CI=true
    depends_on:
      db:
        condition: service_healthy
      # Started, not healthy. src/utils/redis.ts treats Redis as a degraded mode
      # rather than a boot dependency, and .env.example documents running without
      # it as a supported deployment; gating startup on the healthcheck
      # contradicts both, because a Redis that cannot bind its port would then
      # stop the API from serving the routes that never touch Redis. Ordering is
      # still worth keeping: starting redis first means the usual case connects
      # on the first attempt instead of waiting out a watchdog tick.
      redis:
        condition: service_started
    volumes:
      - ${UPLOADS_MOUNT_SOURCE:-app_uploads}:/app/uploads
    # Matches docker-compose.release.yml: Docker's 10s default is under this
    # backend's own worst-case drain (10s to force the HTTP drain, then up to 2s
    # for presence leases and 2s for Redis), so at the default a slow drain is
    # SIGKILLed exactly while handing leases back. 30s clears that ~14s and stays
    # above the 20s shutdown deadline src/index.ts arms (issue #586).
    #
    # No `init: true` here, deliberately. The application is PID 1 — see the CMD
    # in backend/Dockerfile.prod — and installs its own SIGTERM handler, and it
    # spawns no child processes, so there is nothing for an init to reap. tini
    # would only insert itself between the orchestrator and the application, and
    # Docker runs it without `-g`, so it signals just its direct child. That is
    # actively worse here: with a shell still in between it would have produced a
    # fast, clean-looking exit 143 while the drain never ran, hiding #586 rather
    # than fixing it.
    stop_grace_period: 30s

  frontend:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile.prod
      args:
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
    ports:
      - "127.0.0.1:3005:3000"
    environment:
      - NODE_ENV=${NODE_ENV}
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
      - ALLOWED_DEV_ORIGINS=${ALLOWED_DEV_ORIGINS}
      - CI=true
    depends_on:
      - backend

  tunnel:
    image: cloudflare/cloudflared:latest
    restart: always
    command: tunnel --no-autoupdate run
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}
    depends_on:
      - frontend
      - backend

volumes:
  pgdata:
  app_uploads:
